# Hale Lab - Water Body Segmentation
## Environment
### For MacOSX
- Install environment.yml using `conda env create -f environment.yml`

### For Other Platforms
- Install alt_environment.yml using `conda env create -f alt_environment.yml`
